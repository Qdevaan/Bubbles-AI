Extract structured knowledge from the transcript below.

Return JSON of the form:
{
  "entities": [
    {"canonical_name": "...", "entity_type": "person|place|org|topic|task|other",
     "aliases": [], "description": ""}
  ],
  "relations": [
    {"source": "...", "target": "...", "relation": "..."}
  ],
  "events": [
    {"title": "...", "description": "", "due_text": "tomorrow at 3pm",
     "location": ""}
  ],
  "tasks": [
    {"title": "...", "description": "", "priority": "low|medium|high",
     "category": ""}
  ]
}

Rules:
- ``canonical_name`` is lowercase, singular, no honorifics.
- Drop entities that aren't named or specific.
- ``events`` are concrete things happening at a time/place (meetings,
  appointments, deadlines). Skip if no time anchor is implied.
- ``tasks`` are action items the user must do. ``title`` starts with a verb.
- Omit any list that has no extractable items — do not invent.
- Output strictly valid JSON, nothing else.

Transcript:
"""
{{ transcript }}
"""
